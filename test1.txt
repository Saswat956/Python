from aws_utilities.common.utils import sparkconfig, get_spark_session, snow_config, prettify

def compare_lifts():
    # Get a Spark session
    spark = get_spark_session("lift comparison")

    # Define the size variable
    size = "M"

    # Load tables
    dfQ1_lift = spark.table("database.xyz")
    df02_lift = spark.table("database.pqr")

    # Display count and column count
    print("dfQ1_lift", dfQ1_lift.count(), len(dfQ1_lift.columns))
    print("dfQ2_lift", df02_lift.count(), len(df02_lift.columns))

    # Show the first 10 rows of the dataframes
    dfQ1_lift.limit(10).show()
    df02_lift.limit(10).show()

# Call the function to execute the code
compare_lifts()
